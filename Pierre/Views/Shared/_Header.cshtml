<div id="header-main">
  <div class="header-container" id="header">
    <h1>@Html.ActionLink("Pierre Treats", "Index", "Home")</h1> 
  </div>
  <div id="welcome" style="float: right;" >
      @if (User.Identity.IsAuthenticated)
      {
        <br />
        <p>
          Welcome, <span style= "color: #A44A3F" >@User.Identity.Name</span>!<br />
          @Html.ActionLink("Manage Account", "Index", "Account")
        </p>
      }
      else
      {<p>@Html.ActionLink("Log In / Register", "Index", "Account")</p>}
    </div>
</div>
